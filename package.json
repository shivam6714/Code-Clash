{
  "name": "codeclash",
  "version": "1.0.0",
  "description": "CodeClash root project",
  "scripts": {
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:wait": "concurrently -k \"npm run dev:backend\" \"wait-on http-get://localhost:3000/api/health && npm run dev:frontend\"",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "wait-on": "^7.2.0"
  }
}
